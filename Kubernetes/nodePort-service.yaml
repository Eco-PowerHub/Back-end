apiVersion: v1
kind: Service
metadata:
  name: ecoback-service
spec:
  selector:
    app: eco-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30090
  type: NodePort
